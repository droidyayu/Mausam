<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Mausam" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="android:statusBarColor" >@color/purple_500</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.Mausam.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.Clouddy.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.Clouddy.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>